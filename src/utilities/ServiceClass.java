/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 
package utilities;

import java.util.Scanner;

/**
 *
 * @author Deepak
 
public class ServiceClass {
    public static Shapes stringInputMesseage(String data)
    throws IllegalArgumentException{
        /*
        System.out.println("Please enter " + data +":");
        switch(data){
            case("first name"):
                return new Shapes(checkString(new Scanner(System.in).nextLine()));
            case("last name"):
                return new Shapes(checkString(new Scanner(System.in).nextLine()));
            case("address"):
                return new Shapes(checkString(new Scanner(System.in).nextLine()));
            case("city"):
                return new Shapes(checkString(new Scanner(System.in).nextLine()));
            case("province"):
                return new Shapes(checkString(new Scanner(System.in).nextLine()));
            case("age"):
                return new Shapes(new Scanner(System.in).nextInt());
            default:
                return null;
                
        }
        
    }

    /**
     *
     * @param data
     * @return
     
    private static String checkString(String data) 
    throws IllegalArgumentException{
        if(data.trim().isEmpty())
            throw new IllegalArgumentException();
        else
            return data;
    }
}
*/